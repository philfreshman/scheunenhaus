---
import Section from "../components/Section.astro"
import SectionContent from "../components/SectionContent.astro"
import SectionHeader from "../components/SectionHeader.astro"
import { frontmatter as markdown } from "../content/de/furnishing.md"
import { SECTIONS } from "../utils/constants"
---
<style is:global>

</style>

<Section id={SECTIONS.furnishing}>
    <SectionHeader className="section-header" position="left">
        <!-- biome-ignore lint/a11y/useAnchorContent: the heading text is injected by set:html, which Biome cannot see statically -->
        <a href={`#${SECTIONS.furnishing}`} aria-label={markdown.header}>
            <h3 set:html={markdown.header} />
        </a>
    </SectionHeader>
    <SectionContent type="text">
        <p set:html={markdown.content} />
    </SectionContent>
</Section>
